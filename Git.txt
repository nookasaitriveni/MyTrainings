1) Download the repository from Github
2) git init  -->  to initialize a git file in local
8) git remote -v   --> to check remote version or Url config
9) git remote add upstream url --> this is for FIRST time setup pipeline



 ) FIRST time while using pull --> git pull pipeline branch --allow-unrelated-histories
10) git pull upstream branchname
11) git fetch upstream


COMITTING DIRECTLY TO MASTER
3) git status --> to check status of branch and changes and commits
4) # git diff . --> to check changes inside a file 
5) # git diff file/path/name.ext
6) git add .  --> to apply changes to local git
7) git commit -m 'your message'   --> changes we have made
git push pipeline master





New branch develop CREATED
git pull sai master
git fetch sai master
git checkout develop -> changed to existing branch
creating neew branch from develop --> present we are in develop --> git checkout -b newbranch
add changes -- git add -- git commit -- git push pipeline newbranch -- PR creating using url





